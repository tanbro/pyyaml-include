[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=45", "wheel", "setuptools_scm>=6.2"]

[tool.setuptools_scm]
version_scheme = "guess-next-dev" # or post-release
write_to = "src/yamlinclude/version.py"
